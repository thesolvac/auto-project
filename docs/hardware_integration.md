# Hardware Integration (Phase 7 — DEFERRED)

This phase is intentionally deferred and must not be started without explicit
user instruction. This document is filled in when Phase 7 begins.

Planned steps (from `CLAUDE.md`):

1. Flash the ESP32 firmware (compiled in Phase 1) and verify the telemetry
   stream over USB UART.
2. Calibrate the physical camera in its mounted position; update
   `config/camera_calibration.yaml`.
3. Measure and record AprilTag world positions in `config/world_tags.yaml`.
4. Switch `config/runtime.yaml` to `mode: real`.
5. Field-test each phase's acceptance criteria on hardware; document actual vs
   simulated behavior.
6. Record discovered deltas (sim vs real) below.

## Discovered deltas (sim vs real)

_To be filled in during Phase 7._
